%load_ext Cython

%%cython -a
from libc.math cimport log
def func(int i):
    cdef int n
    cdef int nn
    cdef double s = 0
    for n in range(i):
        for nn in range(i):
            s += log(n*nn+1)
    return s

html = last_cell()

def transform():
    return func(i)
