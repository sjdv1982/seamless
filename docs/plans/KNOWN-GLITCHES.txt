Document better: share => readonly=False => celltype not structured (indicate this)

Update guide so it prints out the status weblink

mimetype should trigger re-translation

nice error message if database can't be found

When a transformer creates a dictionary where the values are numpy scalars,
 Seamless says "Transformer exited without result"

Non-reproducible computations give cache misses!

Getting cell.value from within SeamlessTraitlet observe callbacks is unstable (at least within Jupyter)

Project loading with -BAK is misconceived