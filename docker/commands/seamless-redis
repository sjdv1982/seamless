#!/bin/bash
set -u -e

[ ! -d ~/.seamless ] && mkdir ~/.seamless
docker run \
  --rm \
  -p 6379:6379 \
  --name redis-container \
  -v ~/.seamless/redis:/data \
  --user $(id -u):$(id -g) \
  redis redis-server --appendonly yes