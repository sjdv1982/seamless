"""
GUI manager

If we are not master, then GUI manager does not exist; all visualization requests will be forwarded
 By default, to the default sfport
 If a special GUI manager sfport has been specified, then to that one
"""

def init():
    pass
