{% set version = "0.8" %}
package:
    name: seamless-cli
    version: {{ version }}

build:
  number: 1

requirements:
  build:
    - python
  run:
    - openssl
    - ruamel.yaml
about:
  summary: This is a collection of shell scripts wrapping the Seamless Docker image
