"""
minimalist example

no set_global_schema (no globals)
no set_pipeline (single rendermode) => no topology generation other than batching
no define_python (no DAE; should anyway be in raedae module?)

Built-in Raedae types: str, int, float, byte, name, extended_name
"""

define_types
[
  "vec3": {
    "dtype" : ("float", "float", "float"),
    "names" : ("x", "y", "z")
  }
]

define_schema
{
  "name": "batch_nonindexed"
  "value": {
    "command": "str",
    "vertices": {
      "shape"
    }
  }
}
